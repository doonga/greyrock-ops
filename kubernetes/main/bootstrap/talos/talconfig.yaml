---
# yaml-language-server: $schema=https://raw.githubusercontent.com/budimanjojo/talhelper/master/pkg/config/schemas/talconfig.json
clusterName: ${clusterName}
endpoint: "https://${clusterEndpointIP}:6443"

# renovate: depName=ghcr.io/siderolabs/installer datasource=docker
talosVersion: v1.7.1
# renovate: depName=ghcr.io/siderolabs/kubelet datasource=docker
kubernetesVersion: 1.30.0

cniConfig:
  name: none

additionalApiServerCertSans: &san
  - ${clusterName}.${clusterDNSSuffix}
  - "127.0.0.1" # KubePrism
additionalMachineCertSans: *san

nodes:
  - hostname: k8s01.greyrock.io
    ipAddress: 10.1.1.21
    controlPlane: true
    installDisk: /dev/xvda
    networkInterfaces:
      - interface: enxce6e54f0d633
        dhcp: true
        vip:
          ip: ${clusterEndpointIP}

  - hostname: k8s02.greyrock.io
    ipAddress: 10.1.1.22
    controlPlane: true
    installDisk: /dev/xvda
    networkInterfaces:
      - interface: enxc29867e506ac
        dhcp: true
        vip:
          ip: ${clusterEndpointIP}

  - hostname: k8s03.greyrock.io
    ipAddress: 10.1.1.23
    controlPlane: true
    installDisk: /dev/xvda
    networkInterfaces:
      - interface: enx1ee755b5ca64
        dhcp: true
        vip:
          ip: ${clusterEndpointIP}

  - hostname: k8s04.greyrock.io
    ipAddress: 10.1.1.24
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 62:5f:5a:a0:31:f7
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 76:45:95:3e:9d:90
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s05.greyrock.io
    ipAddress: 10.1.1.25
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 52:46:8f:c8:7c:50
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 8e:f6:87:62:48:f9
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s06.greyrock.io
    ipAddress: 10.1.1.26
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: f2:ec:70:30:c9:45
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: b6:bd:38:b0:9a:eb
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s07.greyrock.io
    ipAddress: 10.1.1.27
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: ea:08:6d:14:97:27
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 36:3f:ca:c6:49:4d
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s08.greyrock.io
    ipAddress: 10.1.1.28
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: ae:17:97:79:bc:da
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: fe:59:32:2e:59:73
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s09.greyrock.io
    ipAddress: 10.1.1.29
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 96:ab:c2:56:5a:af
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 32:67:ab:a9:ae:05
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s10.greyrock.io
    ipAddress: 10.1.1.30
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 9e:dd:a3:bb:50:5c
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 06:50:be:2d:75:90
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s11.greyrock.io
    ipAddress: 10.1.1.31
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: c2:20:6a:11:31:f5
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 5e:82:3d:ce:90:f5
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s12.greyrock.io
    ipAddress: 10.1.1.32
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: b2:c0:8c:e1:ad:8f
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: ae:aa:2d:e5:87:53
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s13.greyrock.io
    ipAddress: 10.1.1.33
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 06:67:20:8e:31:cd
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 96:48:e8:0f:df:da
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s14.greyrock.io
    ipAddress: 10.1.1.34
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: aa:89:24:04:ba:a3
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 52:9a:5d:e5:cc:f8
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

  - hostname: k8s15.greyrock.io
    ipAddress: 10.1.1.35
    controlPlane: false
    installDisk: /dev/xvda
    machineDisks:
      - device: /dev/xvdc
        partitions:
          - mountpoint: /var/mnt/sata
    networkInterfaces:
      - interface: bond0
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 3e:42:34:d1:a5:3e
        dhcp: true
      - interface: bond1
        bond:
          mode: active-backup
          deviceSelectors:
            - hardwareAddr: 12:cc:25:28:74:f4
        dhcp: true
        dhcpOptions:
          routeMetric: 4096
    patches:
      - &kubelet_extra_mounts |-
        machine:
          kubelet:
            extraMounts:
              - destination: /var/mnt/sata
                type: bind
                source: /var/mnt/sata
                options:
                  - rbind
                  - rshared
                  - rw

controlPlane:
  schematic:
    customization:
      extraKernelArgs:
        - net.ifnames=1
      systemExtensions:
        officialExtensions:
          - siderolabs/intel-ucode
          - siderolabs/i915-ucode
          - siderolabs/xen-guest-agent

  patches:
    # Disable search domain everywhere
    - |-
      machine:
        network:
          disableSearchDomain: true

    # Force nameserver
    - |-
      machine:
        network:
          nameservers:
            - 10.1.1.11
            - 10.1.1.12

    # Configure NTP
    - |-
      machine:
        time:
          disabled: false
          servers:
            - 10.1.7.2
            - 10.1.7.3
            - 10.1.7.4
            - 10.1.7.5

    # Enable KubePrism
    - |-
      machine:
        features:
          kubePrism:
            enabled: true
            port: 7445

    # Cluster configuration
    - |-
      cluster:
        proxy:
          disabled: true

    # ETCD configuration
    - |-
      cluster:
        etcd:
          advertisedSubnets:
            - 10.1.1.0/24

    # Configure containerd
    - |-
      machine:
        files:
          - op: create
            path: /etc/cri/conf.d/20-customization.part
            content: |
              [plugins]
                [plugins."io.containerd.grpc.v1.cri"]
                  enable_unprivileged_ports = true
                  enable_unprivileged_icmp = true

    # Configure nfs mount options
    - |-
      machine:
        files:
          - op: overwrite
            path: /etc/nfsmount.conf
            permissions: 0o644
            content: |
              [ NFSMount_Global_Options ]
              nfsvers=4.2
              hard=True
              noatime=True
              nodiratime=True
              rsize=131072
              wsize=131072
              nconnect=8

    # Disable default API server admission plugins.
    - |-
      - op: remove
        path: /cluster/apiServer/admissionControl

    # Enable K8s Talos API Access
    - |-
      machine:
        features:
          kubernetesTalosAPIAccess:
            enabled: true
            allowedRoles:
              - os:admin
            allowedKubernetesNamespaces:
              - system-upgrade

    # Kubelet configuration
    - |-
      machine:
        kubelet:
          extraArgs:
            rotate-server-certificates: "true"
          extraConfig:
            maxPods: 150
          nodeIP:
            validSubnets:
                - 10.1.1.0/24

    # Custom sysctls
    - |-
      machine:
        sysctls:
          fs.inotify.max_queued_events: "65536"
          fs.inotify.max_user_instances: "8192"
          fs.inotify.max_user_watches: "524288"
          net.core.rmem_max: "2500000"
          net.core.wmem_max: "2500000"

worker:
  schematic:
    customization:
      extraKernelArgs:
        - net.ifnames=1
      systemExtensions:
        officialExtensions:
          - siderolabs/intel-ucode
          - siderolabs/i915-ucode
          - siderolabs/xen-guest-agent

  patches:
    # Disable search domain everywhere
    - |-
      machine:
        network:
          disableSearchDomain: true

    # Force nameserver
    - |-
      machine:
        network:
          nameservers:
            - 10.1.1.11
            - 10.1.1.12

    # Configure NTP
    - |-
      machine:
        time:
          disabled: false
          servers:
            - 10.1.7.2
            - 10.1.7.3
            - 10.1.7.4
            - 10.1.7.5

    # Cluster configuration
    - |-
      cluster:
        proxy:
          disabled: true

    # Configure containerd
    - |-
      machine:
        files:
          - op: create
            path: /etc/cri/conf.d/20-customization.part
            content: |
              [plugins]
                [plugins."io.containerd.grpc.v1.cri"]
                  enable_unprivileged_ports = true
                  enable_unprivileged_icmp = true

    # Configure nfs mount options
    - |-
      machine:
        files:
          - op: overwrite
            path: /etc/nfsmount.conf
            permissions: 0o644
            content: |
              [ NFSMount_Global_Options ]
              nfsvers=4.2
              hard=True
              noatime=True
              nodiratime=True
              rsize=131072
              wsize=131072
              nconnect=8

    # Kubelet configuration
    - |-
      machine:
        kubelet:
          extraArgs:
            rotate-server-certificates: "true"
          extraConfig:
            maxPods: 150
          nodeIP:
            validSubnets:
                - 10.1.1.0/24

    # Custom sysctls
    - |-
      machine:
        sysctls:
          fs.inotify.max_queued_events: "65536"
          fs.inotify.max_user_instances: "8192"
          fs.inotify.max_user_watches: "524288"
          net.core.rmem_max: "2500000"
          net.core.wmem_max: "2500000"
