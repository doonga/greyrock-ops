---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: k8s-ycl
  namespace: system-controllers
spec:
  interval: 30m
  chart:
    spec:
      chart: k8s-ycl
      version: 0.2.0
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    image:
      repository: ghcr.io/doonga/k8s-ycl
      tag: v0.1.0
