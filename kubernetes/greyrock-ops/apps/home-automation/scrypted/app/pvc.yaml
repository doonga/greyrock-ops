---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrypted-data
  namespace: home-automation
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi

  storageClassName: ceph-block
