---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app homepage-todd
  namespace: selfhosted
spec:
  interval: 15m
  chart:
    spec:
      # renovate: registryUrl=https://homepage.io
      chart: homepage
      version: 2.0.2
      sourceRef:
        kind: HelmRepository
        name: homepage
        namespace: flux-system
  values:
    image:
      repository: ghcr.io/benphelps/homepage
      tag: v0.6.28

    podAnnotations:
      configmap.reloader.stakater.com/reload: &config homepage-config
      secret.reloader.stakater.com/reload: homepage-secret

    service:
      main:
        ports:
          http:
            port: 3000

    ingress:
      main:
        enabled: true
        ingressClassName: internal-nginx
        hosts:
          - host: &host "home-todd.greyrock.casa"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host

    # persistence:
    #   config:
    #     enabled: true
    #     type: custom
    #     volumeSpec:
    #       configMap:
    #         name: *config
    #     subPath:
    #       - path: bookmarks.yaml
    #         mountPath: /app/config/bookmarks.yaml
    #       - path: docker.yaml
    #         mountPath: /app/config/docker.yaml
    #       - path: kubernetes.yaml
    #         mountPath: /app/config/kubernetes.yaml
    #       - path: services.yaml
    #         mountPath: /app/config/services.yaml
    #       - path: settings.yaml
    #         mountPath: /app/config/settings.yaml
    #       - path: widgets.yaml
    #         mountPath: /app/config/widgets.yaml

    # serviceAccount:
    #   create: true

    resources:
      requests:
        cpu: 25m
        memory: 50Mi
    # envFrom:
    #   - secretRef:
    #       name: homepage-secret
