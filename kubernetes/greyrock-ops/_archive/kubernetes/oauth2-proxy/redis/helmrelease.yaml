---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: oauth2-proxy-redis
  namespace: security
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    controller:
      strategy: RollingUpdate

    image:
      repository: public.ecr.aws/docker/library/redis
      tag: 7.2.1@sha256:2a2c7d6d79131e532fbf42a9973eb13a279f6b0b96bf7a44e496a9446549003c

    podAnnotations:
      secret.reloader.stakater.com/reload: oauth2-proxy-redis-secrets

    command: ["redis-server", "--requirepass", "$(REDIS_PASSWORD)"]

    env:
      REDIS_REPLICATION_MODE: master

    envFrom:
      - secretRef:
          name: oauth2-proxy-redis-secrets

    service:
      main:
        ports:
          http:
            enabled: false
          redis:
            enabled: true
            port: 6379

    resources:
      requests:
        cpu: 15m
        memory: 64M
      limits:
        memory: 128M
