expose:
  type: clusterIP
  tls:
    enabled: true
    certSource: secret
    secret:
      secretName: harbor-greyrock-io-tls
ipFamily:
  ipv6:
    enabled: false
persistence:
  enabled: true
  resourcePolicy: keep
externalURL: https://harbor.greyrock.io
secretKey: ${secretKey}
internalTLS:
  enabled: true
