---
- name: Add group
  ansible.builtin.group:
    name: timemachine

- name: Add user
  ansible.builtin.user:
    name: "{{ tm_todd_desktop_user }}"
    password: '!'
    create_home: false
    groups: timemachine
    append: yes
