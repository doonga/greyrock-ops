---
- name: run zfs_exporter
  containers.podman.podman_container:
    name: zfs_exporter
    image: "ghcr.io/doonga/zfs_exporter:2.2.8@sha256:87d3281fea76a1d3718324ed8e9de4b333b97ab25c60f65367bab7db3a98d608"
    state: started
    privileged: true
    network: host
    generate_systemd:
      path: "/home/{{ ansible_user }}/.config/systemd/user"
      restart_policy: always
      time: 120
      names: true
      container_prefix: ansible
  become: false

- name: enable podman-systemd | zfs_exporter
  ansible.builtin.systemd:
    name: ansible-zfs_exporter
    enabled: true
    scope: user
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ ansible_user_uid }}"
  remote_user: todd
  become: false
