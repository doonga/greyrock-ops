---
- name: run zfs_exporter
  containers.podman.podman_container:
    name: zfs_exporter
    image: "ghcr.io/doonga/zfs_exporter:2.2.8@sha256:f97a3ec76e7009179485359da29c441822c2774ec724af029b3ec259c909c86d"
    state: started
    privileged: true
    network: host
    generate_systemd:
      path: "/home/{{ ansible_user }}/.config/systemd/user"
      restart_policy: always
      time: 120
      names: true
      container_prefix: ansible
  become: false

- name: enable podman-systemd | zfs_exporter
  ansible.builtin.systemd:
    name: ansible-zfs_exporter
    enabled: true
    scope: user
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ ansible_user_uid }}"
  remote_user: todd
  become: false
